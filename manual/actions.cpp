#include "actions.h"

Actions::Actions(QAction *parent)
    :QAction(parent)
{
    //exitAction = new QAction(tr("ÍË³ö(&X"), this);
    //exitAction->setShortcut(tr("Ctrl+Q"));
    //connect(exitAction, SIGNAL(triggered()), this, SLOT(close()));
}
